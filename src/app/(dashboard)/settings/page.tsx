// TODO: Settings screen
// Planned: change password, notification preferences, language.

export default function SettingsPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold mb-6">Configurações</h1>
      <div
        className="rounded-xl p-8 shadow-sm flex items-center justify-center h-64"
        style={{ backgroundColor: "var(--bg-card)" }}
      >
        <p style={{ color: "var(--text-soft)" }}>Em desenvolvimento</p>
      </div>
    </div>
  );
}
